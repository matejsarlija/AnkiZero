package com.example.ankizero.ui.theme

import androidx.compose.ui.graphics.Color

// Original Material Colors
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Enhanced Grayish Noise Colors with better contrast and warmth
val LightGrayNoise = Color(0xFFF7F8FA)      // Slightly warmer, more modern
val MidGrayNoise = Color(0xFFE2E8F0)        // Better contrast
val DarkGrayNoise = Color(0xFFCBD5E0)       // More sophisticated
val SubtleDarkerGrayNoise = Color(0xFFA0AEC0) // Better readability

// Enhanced Success Colors (for "Got It" button)
val SuccessGreen = Color(0xFF48BB78)         // Modern green
val SuccessGreenDark = Color(0xFF2F855A)     // Dark mode variant
val SuccessGreenLight = Color(0xFF68D391)    // Light accent

// Enhanced Error/Retry Colors (for "Try Again" button)
val RetryRed = Color(0xFFFC8181)            // Softer red
val RetryRedDark = Color(0xFF742A2A)        // Dark mode variant
val RetryRedLight = Color(0xFFFEB2B2)       // Light accent

// Accent Colors for better visual hierarchy
val AccentBlue = Color(0xFF4299E1)          // Primary accent
val AccentBlueDark = Color(0xFF2B6CB0)      // Dark variant
val AccentBlueLight = Color(0xFF90CDF4)     // Light variant

val AccentPurple = Color(0xFF9F7AEA)        // Secondary accent
val AccentPurpleDark = Color(0xFF553C9A)    // Dark variant
val AccentPurpleLight = Color(0xFFB794F6)   // Light variant

// Background Gradients
val BackgroundStart = Color(0xFFF8F9FA)     // Light gradient start
val BackgroundMid = Color(0xFFE9ECEF)       // Light gradient middle
val BackgroundEnd = Color(0xFFDEE2E6)       // Light gradient end

val BackgroundStartDark = Color(0xFF1A1A2E) // Dark gradient start
val BackgroundMidDark = Color(0xFF16213E)   // Dark gradient middle
val BackgroundEndDark = Color(0xFF0F3460)   // Dark gradient end

// Card Surface Colors
val CardSurfaceLight = Color(0xFFFFFFFF)    // Pure white for cards in light mode
val CardSurfaceDark = Color(0xFF2D3748)     // Sophisticated dark card surface
val CardSurfaceAccentLight = Color(0xFFFAFAFA) // Subtle accent surface
val CardSurfaceAccentDark = Color(0xFF4A5568)  // Dark accent surface

// Progress Indicator Colors
val ProgressIndicatorLight = Color(0xFF4299E1)
val ProgressIndicatorDark = Color(0xFF90CDF4)

// Shadow and Overlay Colors
val ShadowLight = Color(0x1A000000)         // 10% black for light mode shadows
val ShadowDark = Color(0x33FFFFFF)          // 20% white for dark mode shadows
val OverlayLight = Color(0x0DFFFFFF)        // 5% white overlay
val OverlayDark = Color(0x1A000000)         // 10% black overlay

// Interactive State Colors
val HoverStateLight = Color(0x0A4299E1)     // Subtle blue hover
val HoverStateDark = Color(0x1A90CDF4)      // Subtle blue hover dark
val PressedStateLight = Color(0x1A4299E1)   // Pressed state
val PressedStateDark = Color(0x2A90CDF4)    // Pressed state dark

// Semantic Colors for better UX
val InfoColor = Color(0xFF3182CE)           // Information blue
val WarningColor = Color(0xFFD69E2E)        // Warning amber
val ErrorColor = Color(0xFFE53E3E)          // Error red
val SuccessColor = Color(0xFF38A169)        // Success green

// Text Colors with better hierarchy
val TextPrimary = Color(0xFF2D3748)         // Primary text
val TextSecondary = Color(0xFF718096)       // Secondary text
val TextTertiary = Color(0xFFA0AEC0)        // Tertiary text
val TextPrimaryDark = Color(0xFFF7FAFC)     // Dark mode primary
val TextSecondaryDark = Color(0xFFE2E8F0)   // Dark mode secondary
val TextTertiaryDark = Color(0xFFCBD5E0)    // Dark mode tertiary